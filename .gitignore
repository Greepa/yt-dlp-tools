# ============================================================
#  Secrets: NEVER commit (api / cookie / token / keys / credentials)
#  密钥文件：绝不上传
# ============================================================
cookies.txt
*.env
.env
.env.*
*.env.*
*token*
*secret*
*credential*
.credentials
.credentials.*
*.pem
*.key
*.p12
*.pfx
*.keystore
*.jks

# ============================================================
#  Personal input / output (not part of the reusable toolkit)
#  个人输入/输出：不属于通用工具包
# ============================================================
urls.txt
video_stats.csv
archive.txt
downloads/

# ============================================================
#  Binaries (downloaded on demand via setup.bat)
#  二进制：用 setup.bat 现下（ffmpeg 单文件 >100MB，超 GitHub 限制）
# ============================================================
ffmpeg.exe
ffprobe.exe
yt-dlp.exe

# ============================================================
#  Temp / logs
# ============================================================
*.log

test.txt
