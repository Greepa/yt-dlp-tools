# yt-dlp 使用说明（已按你本机环境实测调好）

**你的环境现状（2026-09-05 实测）：**

| 项目 | 状态 |
|---|---|
| 可执行文件 | `E:\yt-dlp.exe`，版本 `2026.08.19` ✅ |
| ffmpeg + ffprobe | ✅ 已装好（`E:\yt-dlp-tools\`，2026-09-04 最新版） |
| Node.js | ✅ 已装（v22，YouTube 解密签名需要它） |
| B 站 | ⚠️ 能用，但**必须配 cookie**，否则会 412 限流（见第二节） |
| YouTube / X(Twitter) | ❌ 直连不通，**必须挂代理** |

---

## 一、最快上手：双击即可

进入 `E:\yt-dlp-tools\`，双击对应脚本：

| 文件名 | 作用 |
|---|---|
| `set_cookie.bat` | **配置 B站 cookie（先跑这个，3 秒解决 412 限流）** |
| `download.bat` | 下载单个视频（粘贴链接回车） |
| `batch_download.bat` | 批量下载（先填 `urls.txt`，一行一个链接） |
| `info.bat` | 只看元数据不下载（播放量/点赞/评论/时长） |
| `batch_info.bat` | 批量采集统计 → `video_stats.csv`（**爆款分析主力工具**） |
| `subtitles.bat` | 只下载字幕（分析脚本用） |
| `update.bat` | 自更新 yt-dlp（下载失败时先跑它） |
| `install_ffmpeg.bat` | 重装 ffmpeg（已装好，一般不用跑） |
| `list_sites.bat` | 导出支持站点清单到 `supported_sites.txt` |

下载的文件统一放在 `E:\yt-dlp-tools\downloads\` 下，按「UP主/标题」分文件夹。
同时会保存：`.info.json`（元数据）、封面图、字幕文件。

> **已通过端到端实测**（B 站视频）：视频下载 → ffmpeg 音视频合并为 mp4 →
> 写入元数据 → 保存封面，全流程正常。实测生成的文件名：
> ```
> downloads\索尼音乐中国\【官方 MV】Never Gonna Give You Up - Rick Astley [BV1GJ411x7h7].mp4
> ```
> 中文 UP 主名和标题都能正常保留 —— 配置里已禁用 `--restrict-filenames`，
> 否则中文会被替换成一串下划线。

---

## 二、配置 B 站 cookie（强烈建议，3 秒搞定）

**这一步解决 B 站 412 限流，只要做一次。**

### 为什么要 cookie

我做了对照测试，结论很干脆：

| 请求方式 | 结果 |
|---|---|
| 不带 cookie | ❌ `HTTP Error 412`，拿不到任何信息 |
| 带 cookie | ✅ 立刻成功，拿到完整画质列表 |

而且 **`--extractor-retries` 对 412 完全无效**（实测重试 10 次照样全失败），
降频参数也只能缓解、不能根治。所以 cookie 是唯一靠谱的解法。

### 好消息：不需要登录，也没有隐私问题

我一开始用的是一组**完全伪造的** cookie，结果同样通过了风控。
说明 B 站拦截的主要是「完全没有 cookie 的匿名请求」——
只要请求带了 cookie（哪怕是游客 cookie）就放行。

所以你**不用登录、不用担心泄露账号**，直接复制浏览器的游客 cookie 就行。

> 只有想拿「1080P 高码率 / 1080P 高清」这类会员画质时，才需要登录态的 cookie。

### 操作步骤

**① 复制 cookie**

1. 浏览器打开 `bilibili.com`（登不登录都行）
2. 按 `F12` → 切到 **Console（控制台）** 标签
3. 粘贴这一行，回车：

   ```js
   copy(document.cookie)
   ```

4. cookie 已在剪贴板里了

**② 写入配置**

双击 `set_cookie.bat`，按任意键。脚本会自动：
从剪贴板读取 → 转成 Netscape 格式存为 `cookies.txt` → 启用配置里的 `--cookies` 行。

也可以把 cookie 字符串直接发给我，我来配。

**③ 验证**

```bat
E:\yt-dlp.exe --config-location E:\yt-dlp-tools\yt-dlp.conf --simulate -F "B站链接"
```

能列出画质列表就说明通了。

---

## 三、ffmpeg（已装好）

装在 `E:\yt-dlp-tools\`，并且已经在 `yt-dlp.conf` 里通过 `--ffmpeg-location` 指好了路径，
所以这些功能**现在就能用**：

- 1080p 音视频合并 → 输出 mp4
- 字幕自动转 srt
- 转 mp3、内嵌元数据

想再开启更多（字幕内嵌、封面内嵌、只提取音频），打开 `yt-dlp.conf` 末尾，
把对应行前面的 `#` 删掉即可。

---

## 四、命令行直接用法（不用脚本也行）

打开 PowerShell 或 CMD。**注意必须用 Windows 路径**（`E:\` 这种），
不能用 Git Bash 的 `/e/` 路径——yt-dlp 是 Windows 程序，不认 `/e/`。

```bat
:: 最简下载
E:\yt-dlp.exe "视频链接"

:: 套用我配好的配置（推荐，含UA伪装/cookie/降频/ffmpeg）
E:\yt-dlp.exe --config-location E:\yt-dlp-tools\yt-dlp.conf "视频链接"

:: 只看有哪些画质可选
E:\yt-dlp.exe -F "视频链接"

:: 指定画质（例如 720p）
E:\yt-dlp.exe -f "bv[height<=720]+ba/b" "视频链接"

:: 下载整个播放列表
E:\yt-dlp.exe --yes-playlist "播放列表链接"

:: 只下载音频
E:\yt-dlp.exe -x --audio-format mp3 "视频链接"

:: 挂代理下 YouTube（7890 换成你的代理端口）
E:\yt-dlp.exe --proxy http://127.0.0.1:7890 "YouTube链接"
```

**常用参数速查：**

| 参数 | 作用 |
|---|---|
| `-F` | 列出所有可用画质 |
| `-f <规则>` | 指定画质，如 `bv+ba`、`bv[height<=1080]+ba` |
| `-o <模板>` | 输出文件名模板 |
| `-a 文件` | 批量下载，从文件读链接 |
| `--skip-download` | 不下载，只取信息 |
| `--write-info-json` | 保存元数据 JSON |
| `--write-auto-subs` | 下载自动字幕 |
| `--download-archive 文件` | 记录已下载，避免重复 |
| `--cookies 文件` | 用 cookie 文件（已配好） |
| `--proxy 地址` | 走代理 |
| `-U` | 更新 yt-dlp |

---

## 五、四个实测踩出来的坑

### 坑 1：YouTube / X 必须挂代理

实测你本机直连 YouTube 超时。需要 Clash 之类的工具，然后加 `--proxy`：

```bat
E:\yt-dlp.exe --proxy http://127.0.0.1:7890 "YouTube链接"
```

`7890` 换成实际端口（Clash 默认 7890，v2ray 常见 10809）。
不想每次敲，就在 `yt-dlp.conf` 末尾把这行的 `#` 去掉：

```
--proxy http://127.0.0.1:7890
```

> 实测你目前**没有开任何代理**（7890/10809/7897/1080 等常见端口全是关闭的），
> 所以下 YouTube 前得先启动代理软件。

### 坑 2：B 站 412 限流 —— cookie 是唯一根治办法

无 cookie 时，B 站的 IP 配额**极紧**，实测规律：

- 成功下载 1 个视频后，紧接着再请求就会 412
- 限流会自己恢复，但要等十几到几十分钟
- `--extractor-retries` 无效，`--sleep-requests` 等降频参数也只能缓解

**解法：配 cookie，见第二节。** 配好后基本不会再遇到 412。

即使偶尔还受限，分批下即可——`batch_download.bat` 带了 `--download-archive`，
已下过的会自动跳过，适合「跑一批、歇一会、再跑」。

### 坑 3：B 站 1080P 高码率要大会员

游客 cookie 最高只能拿到 1080P（普通）/ 720P。想要 1080P 高码率、1080P 高清，
必须是**大会员登录态**的 cookie——也就是复制 cookie 时要处于登录状态。

所以：
- 只想正常下载 → 游客 cookie 就够（无隐私风险，见第二节）
- 想要最高画质 → 复制 cookie 前先登录 B 站

备选方案是用浏览器 cookie 直读：

```bat
E:\yt-dlp.exe --cookies-from-browser edge "B站链接"
```

> ⚠️ 这种方式**必须彻底退出浏览器**（托盘图标也要退），否则报
> `Could not copy Chrome cookie database`——浏览器锁住了 cookie 数据库。
> 这也是我推荐用 `set_cookie.bat` 的原因，它不需要关浏览器。

### 坑 4：配置文件的两个编码/转义陷阱（已帮你处理）

**① 编码必须是 GBK/ANSI**

yt-dlp 在中文 Windows 上用 GBK 读配置文件。如果存成 UTF-8 且含中文，会直接报：

```
error: Unable to parse "...": 'gbk' codec can't decode byte 0xa1 in position 91
```

`yt-dlp.conf` 已存为 GBK。
**如果你要改它，改完务必用 GBK/ANSI 保存**（记事本「另存为」→ 编码选 **ANSI**）。

**② 路径里的反斜杠会被吃掉**

配置文件会把 `\` 当转义符处理。写 `E:\yt-dlp-tools` 会变成 `E:yt-dlp-tools`，
导致 ffmpeg 找不到。所以配置里**路径一律用正斜杠**：

```
--ffmpeg-location <工具目录>     ← 正确
--ffmpeg-location E:\yt-dlp-tools     ← 错误，反斜杠会被吞
```

已经帮你用正斜杠写好了，改配置时注意保持。

---

## 六、针对你的「无刷电机爆款视频分析」场景

推荐流程：

1. **先配 cookie**（第二节），否则批量请求会卡在 412
2. **批量采集数据**：候选视频链接填进 `urls.txt`，双击 `batch_info.bat`
   → 得到 `video_stats.csv`（播放量、点赞、评论、时长、UP主、发布日期）
3. **Excel 排序筛选**：按播放量降序筛爆款；算「点赞/播放」「评论/播放」互动率
4. **拉取爆款素材**：Top N 链接填进 `urls.txt`，双击 `batch_download.bat`
5. **分析脚本**：双击 `subtitles.bat` 拿字幕，分析开头 3 秒钩子、节奏、结构

CSV 里标题被我放在**最后一列**，这是故意的——标题常含逗号，放最后不会打乱前面的列。

---

## 七、可选：设为全局命令

现在必须输完整路径 `E:\yt-dlp.exe`。想在任意位置直接敲 `yt-dlp`，
用管理员 PowerShell 跑一次：

```powershell
[Environment]::SetEnvironmentVariable('Path', $env:Path + ';E:\', 'Machine')
```

另外，把配置复制成 `C:\Users\user\.config\yt-dlp\config`，yt-dlp 会**自动加载**，
连 `--config-location` 都不用写。

> ⚠️ 你这里**已存在一个全局配置文件**（内容 `--js-runtimes node`，
> 应该是之前装 Agent Reach 留下的，用于 YouTube 签名解密）。
> 覆盖前建议先备份，或者把它的内容合并进去。

---

## 八、出问题先做这两件事

1. 双击 `update.bat` 更新 yt-dlp —— 视频网站反爬策略经常变，旧版本容易失效
2. 加 `--verbose` 看详细日志

```bat
E:\yt-dlp.exe --config-location E:\yt-dlp-tools\yt-dlp.conf --verbose "链接"
```

看到 `exe versions: ffmpeg ... ffprobe ...` 说明 ffmpeg 正常挂载。

---

## 九、上游项目与开源合规

- **下载引擎 yt-dlp**：[https://github.com/yt-dlp/yt-dlp](https://github.com/yt-dlp/yt-dlp)，采用 **Unlicense（公共领域）** 许可，可自由使用、修改、再分发。
- **ffmpeg**：[https://ffmpeg.org](https://ffmpeg.org)，由 `install_ffmpeg.bat` 从其官方构建页下载。
- **本仓库定位**：是 yt-dlp 的**二次封装**，只提供配置 + 脚本 + 文档，**不改 yt-dlp 源码**；`yt-dlp.exe` / ffmpeg 都是运行时现下，不进仓库。
- **使用边界**：下载内容请在目标站点服务条款与当地法律允许范围内使用，作者不对下载用途负责。

---

## 十、给智能体 / WorkBuddy 的配置指引（指令式）

如果你是一个能读文件、能执行命令的智能体，按下面做即可：

1. 读 `README.md` 和这份 `使用说明.md`，了解脚本与已知坑。
2. 在本目录执行 `setup.bat`（或先放 `yt-dlp.exe` 再执行 `install_ffmpeg.bat`）。
3. B 站下载：执行 `set_cookie.bat` 配 cookie；若已拿到 cookie 字符串，可手动写 Netscape 格式 `cookies.txt` 并取消 `yt-dlp.conf` 中 `--cookies` 注释。
4. 调用：`cd` 到本目录后执行 `yt-dlp.exe --config-location yt-dlp.conf "<URL>"`。
5. 必看约束：
   - **配置编码**：中文 Windows 上 yt-dlp 用 **GBK/ANSI** 读 `yt-dlp.conf`。改配置务必 ANSI 保存；仓库内置 conf 是纯 ASCII，跨平台安全。
   - **正斜杠路径**：配置里用 `/` 不是 `\`。
   - **YouTube/X 需代理**：本仓库不自带代理。
   - **密钥勿上传**：`cookies.txt` / `.env` / `*token*` 已被 `.gitignore` 排除。
